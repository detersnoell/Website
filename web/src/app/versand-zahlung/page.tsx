import { LegalStub } from "@/components/LegalStub";

export const metadata = { title: "Versand & Zahlung", robots: { index: false } };

export default function Page() {
  return <LegalStub title="Versand & Zahlung" />;
}
