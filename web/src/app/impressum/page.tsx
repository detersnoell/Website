import { LegalStub } from "@/components/LegalStub";

export const metadata = { title: "Impressum", robots: { index: false } };

export default function Page() {
  return <LegalStub title="Impressum" />;
}
